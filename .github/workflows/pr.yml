name: "PR"
on:
  pull_request:
    types:
      - opened
    branches:
      - 'dependabot/**'
jobs:
  pr-target:
    name: pr-target
    runs-on: ubuntu-latest

    steps:
      - name: "ECHO"
        run: echo "running pr"